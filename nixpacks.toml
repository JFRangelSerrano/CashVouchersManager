[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
    "dotnet publish CashVouchersManager.API/CashVouchersManager.API.csproj -c Release -o /app/out --self-contained false --runtime linux-x64"
]

[start]
cmd = "cd /app/out && ./CashVouchersManager.API --urls http://0.0.0.0:$PORT"
